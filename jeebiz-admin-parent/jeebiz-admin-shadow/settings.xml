<?xml version="1.0" encoding="UTF-8"?>

<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <!--本地仓库。该值表示构建系统本地仓库的路径。其默认值为~/.m2/repository。 -->
    <!--    <localRepository>/root/.m2/repository</localRepository>-->

    <!--Maven 是否需要和用户交互以获得输入。如果 Maven 需要和用户交互以获得输入，则设置成 true，反之则应为 false。默认为
                    true。 <interactiveMode>true</interactiveMode>
    -->
    <servers>
        <!-- 请妥善保管好您的配置，不要随意分享给他人 -->
        <server>
            <id>jeebiz-services-maven</id>
            <username>${env.CODING_ARTIFACTS_USERNAME}</username>
            <password>${env.CODING_ARTIFACTS_PASSWORD}</password>
        </server>
    </servers>

    <mirrors>
        <mirror>
            <id>nexus-tencentyun</id>
            <!-- 此配置避免了本仓库制品的拉取流量被切换到腾讯云镜像源，保证您在使用镜像加速的同时可以从本仓库拉取制品 -->
            <mirrorOf>!jeebiz-services-maven</mirrorOf>
            <name>Nexus tencentyun</name>
            <url>http://mirrors.cloud.tencent.com/nexus/repository/maven-public/</url>
        </mirror>
    </mirrors>

    <!-- 环境配置，作为企业内应用，只使用内网私库（默认） 和 阿里云（私库维护时） -->
    <profiles>
        <profile>
            <id>Repository Proxy</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <repositories>
                <repository>
                    <id>jeebiz-services-maven</id>
                    <name>maven</name>
                    <url>https://jeebiz-maven.pkg.coding.net/repository/services/maven/</url>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                </repository>
            </repositories>
        </profile>
    </profiles>

    <!-- 默认生效 -->
    <activeProfiles>
        <activeProfile>Repository Proxy</activeProfile>
    </activeProfiles>

</settings>